// Primitive

// 7 data types: String, Number, Boolean, null, undefined, Symbol, BigInt
// javascript = dynamically typed language
// Typescript = statically typed language 

//Reference (non primitive) - Array, object, functions
//Learn about typeof operator in javascript in detail 


const score = 100;
const scoreValue = 100.3
const isLoggedIN = false 

const id = Symbol('231');

const anotherId = Symbol('231');

console.log(id === anotherId); // false, because symbols are unique